pipeline:
  name: broken-validation
  description: This pipeline has a validation error for demonstration

stages:
  - build
  - test

compile-job:
  stage: build
  image: python:3.12-slim
  script:
    - echo "building..."

test-job:
  stage: test
  image: python:3.12-slim
  needs: [compile-job, nonexistent-job]   # ← ERROR: nonexistent-job does not exist in the same stage
  script:
    - pytest tests/